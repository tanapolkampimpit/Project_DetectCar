version: '3.8'

services:
  backend:
    build: .
    container_name: car-detect-backend
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    environment:
      - ENV=production
    restart: always
